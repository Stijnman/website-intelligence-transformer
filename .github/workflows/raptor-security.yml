name: RAPTOR Security

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - "main"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  raptor:
    if: ${{ github.event_name == 'workflow_dispatch' || !github.event.repository.private }}
    uses: Stijnman/Stijnman/.github/workflows/raptor-reusable.yml@main
    with:
      run_sca: true
